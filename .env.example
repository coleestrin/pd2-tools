# Optional overrides for docker-compose.yml / docker-compose.dev.yml.
# You can run `docker compose up --build` without copying this file if the
# defaults below work for you.

POSTGRES_DB=pd2_tools
POSTGRES_USER=pd2_user
POSTGRES_PASSWORD=password

API_PORT=3000
WEB_PORT=4173
VITE_API_URL=http://localhost:3000/api/v1

API_VERSION=v1
CURRENT_SEASON=13
CORS_ORIGIN=http://localhost:4173
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=1000
LOG_LEVEL=info
